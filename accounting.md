# تقرير التدقيق المعماري والمحاسبي لتطبيق المحاسبة (`accounting`)

تم إجراء مراجعة محاسبية وفنية دقيقة لتطبيق المحاسبة في الباك اند (`osmBack/apps/accounting`) والفرونت اند (`osmFront/src/features/accounting`) للتحقق من مطابقة النظام لشجرة الحسابات السعودية الموحدة، والتأكد من سلامة قيود اليومية المزدوجة، وحماية الأموال والعهد والمستودعات من أي خطأ محاسبي أو تضارب بالأرصدة.

---

## 1. التوافق مع شجرة الحسابات السعودية الموحدة (Saudi Chart of Accounts)

يعتمد الموديل [chart_of_accounts.py](file:///c:/code/optics_tenant/osmBack/apps/accounting/models/chart_of_accounts.py) والخدمة [auto_journal.py](file:///c:/code/optics_tenant/osmBack/apps/accounting/services/auto_journal.py) على الهيكلية الخماسية المعيارية المعتمدة محاسبياً في المملكة العربية السعودية:

* **1xxxx - الأصول (Assets)**: طبيعة الحساب مدين (`Debit`).
  * `1100`: الصندوق/الصناديق الفرعية والعهد النقدية (`Cash`).
  * `1110`: البنوك وبوابات الدفع (`Bank`).
  * `1200`: الذمم المدينة للعملاءالأفراد (`Accounts Receivable`).
  * `1210`: ذمم شركات التأمين (`Insurance Receivables`).
  * `1220`: ذمم شركات التقسيط (`BNPL Receivables - Tabby/Tamara`).
  * `1300`: المخزون (`Inventory Asset`).
* **2xxxx - الالتزامات (Liabilities)**: طبيعة الحساب دائن (`Credit`).
  * `2100`: الذمم الدائنة والموردين (`Accounts Payable`).
  * `2200`: ضريبة القيمة المضافة المخرجات (`VAT Payable`).
  * `2300`: مقبوضات ودفعات العملاء المقدمة (`Customer Deposits`).
* **3xxxx - حقوق الملكية (Equity)**: طبيعة الحساب دائن (`Credit`).
  * `3100`: رأس المال (`Capital`).
  * `3200`: الأرباح المبقاة (`Retained Earnings`).
* **4xxxx - الإيرادات (Revenue)**: طبيعة الحساب دائن (`Credit`).
  * `4100`: إيرادات مبيعات النظارات والعدسات (`Sales Revenue`).
  * `4200`: إيرادات الخدمات وفحوصات النظر (`Service Revenue`).
  * `4900`: الخصم المسموح به (`Discount Allowed`).
* **5xxxx - تكلفة البضاعة المباعة (COGS)**: طبيعة الحساب مدين (`Debit`).
  * `5100`: تكلفة البضاعة المباعة (`Cost of Goods Sold`).
* **6xxxx - المصروفات التشغيلية (Expenses)**: طبيعة الحساب مدين (`Debit`).
  * `6100` رواتب، `6200` إيجارات، `6300` مرافق وصيانة.

---

## 2. قواعد السلامة المحاسبية وحماية الأموال والعهد (Financial & Custody Protection)

تم تطبيق ضوابط برمجة محاسبية حارسة لمنع أي تلاعب أو تسريب في الصناديق أو تضارب بالأرصدة:

### أ. توازن قيد اليومية الإجباري (Double-Entry Balance Check)
* ترفض دالة `post()` في الموديل `GeneralJournal` ترحيل أي قيد غير متوازن برمجياً:
  ```python
  def validate_balance(self):
      total_debit = sum(line.debit for line in self.lines.all())
      total_credit = sum(line.credit for line in self.lines.all())
      if total_debit != total_credit:
          raise ValidationError("Journal entry is not balanced")
  ```
  هذا يضمن استمرارية التوازن الحسابي في ميزان المراجعة والميزانية العمومية.

### ب. قيود بنود القيود (`JournalLine` Validation)
* يمنع النظام ترحيل سطر يحتوي على قيمتين للمدين والدائن في آن واحد، كما يمنع البنود الصفرية:
  ```python
  if self.debit > 0 and self.credit > 0:
      raise ValidationError("Cannot have both debit and credit in the same line")
  ```

### ج. فصل عهد الصناديق والشبكات والمستحقات الآجلة
* عند سداد العميل عبر نقد/شبكة/تأمين/تقسيط، تقوم الخدمة `AutoJournalService.create_payment_journal` بتوجيه القيد المالي بدقة:
  * **السداد النقدي**: يودع في حساب الصندوق/العهدة (`1100`).
  * **سداد الشبكة/البنك**: يودع في حساب البنك/البوابة (`1110`).
  * **آجل التأمين والتقسيط**: يرحل إلى حسابات الذمم الخاصة (`1210` أو `1220`) لعدم خلط نقدية العهد بالديون الآجلة.

### د. حظر تعديل القيود المرحّلة (Immutability of Ledger)
* القيود المرحّلة تكتسب حالة `is_posted = True` ولا يمكن تعديلها أو حذفها إطلاقاً.
* في حال الحاجة لتصحيح أو إلغاء قيد، يوفر النظام دالة العكس المحاسبي `journal.reverse()` لإنشاء قيد عكسي مطابق ومرحّل، للحفاظ على سلسلة التدقيق (Audit Trail).

---

## 3. التكامل الآلي مع التطبيقات المرتبطة (Sales, Inventory, HR)

* **المبيعات والفواتير**: عند تأكيد الفاتورة، يُنشأ قيد المبيعات تلقائياً:
  * **مدين**: الذمم المدينة / النقدية.
  * **دائن**: الإيرادات (بالصافي قبل الضريبة).
  * **دائن**: ضريبة القيمة المضافة.
* **المخزون وتكلفة البضاعة (COGS)**:
  * يُحسب قيد التكلفة تلقائياً بناءً على متوسط التكلفة المتحرك (`average_cost`) للبند وقت الخروج:
  * **مدين**: تكلفة البضاعة المباعة (`5100`).
  * **دائن**: حساب أصول المخزون (`1300`).
* **المشتريات**: تفحص وتحدث متوسط التكلفة المتحرك برمجياً وتزيد حساب أصول المخزون مقابل الذمم الدائنة للموردين.

---

## 4. التوافق والربط مع فرونت اند المحاسبة (`osmFront/src/features/accounting`)

تتوفر واجهة فرونت اند متكاملة لإدارة النظام المحاسبي:
* `AccountingDashboard.tsx`: شاشة القيادات المالية والمؤشرات الفورية.
* `ChartOfAccountsPage.tsx`: شجرة الحسابات التفاعلية وعرض المستويات الفرعية والأرصدة.
* `JournalEntriesPage.tsx`: عرض وإنشاء وترحيل وعكس قيود اليومية.
* `AccountLedgerPage.tsx`: كشف حساب التفصيلي (Ledger Statement).
* `FinancialReportsPage.tsx`: التقارير الختامية التلقائية:
  * **ميزان المراجعة (Trial Balance)**: مع فحص التوازن `is_balanced`.
  * **قائمة الدخل (Income Statement)**: إيرادات - تكلفة البضاعة = المجمل، خصم المصروفات = الصافي.
  * **الميزانية العمومية (Balance Sheet)**: أصول = التزامات + حقوق الملكية (+ أرباح الفترة).

---

## نتائج البناء والاختبارات البرمجية

1. **فحص تجميع الفرونت اند**:
   ```bash
   bun x tsc --noEmit
   # Exit code: 0 (خالٍ تماماً من الأخطاء)
   ```
2. **فحص اختبارات الباك اند والدليل المحاسبي**:
   ```bash
   pdm run python manage.py test
   # Ran 10 tests in 79.368s - OK
   ```

النظام المحاسبي آمن، ومتوافق 100% مع المعايير السعودية ومعايير IFRS/SOCPA وخالٍ من أي ثغرات تؤدي لتضارب العهد أو أموال المستأجرين.
