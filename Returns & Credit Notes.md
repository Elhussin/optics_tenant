# تقرير تدقيق فواتير المرتجعات والإشعارات الدائنة (`Returns & Credit Notes`)

تم إجراء تدقيق برمجي ومحاسبي شامل لميزات **الفواتير ومرتجعات المبيعات والمشتريات والإشعارات الدائنة** في الباك اند (`osmBack/apps/sales` و `osmBack/apps/accounting`) والفرونت اند (`osmFront/src/features/invoices`).

---

## 1. إمكانية إدخال فواتير المرتجعات والإشعارات الدائنة

**نعم، النظام يدعم بالكامل إدخال ومعالجة فواتير المرتجعات للموردين والعملاء، بالإضافة إلى الإشعارات الدائنة (Credit Notes) المطابقة لمتطلبات هيئة الزكاة والضريبة والجمارك (ZATCA).**

يتم تصنيف الفواتير في النظام عبر الموديل `InvoiceType` باستخدام حقل نوع الحركة التشغيلية `ActionType`:
1. **`sale`**: فاتورة مبيعات عميل (تخصم من المخزون، وتثبت الإيراد والذمم المدينة).
2. **`purchase`**: فاتورة مشتريات مورد (تزيد المخزون وتحدث متوسط التكلفة المتحرك، وتثبت الذمم الدائنة).
3. **`return_sale`**: فاتورة مرتجع مبيعات عميل (تعيد البضاعة للمخزون، وتعكس الإيراد والذمم/النقود).
4. **`return_purchase`**: فاتورة مرتجع مشتريات للمورد (تخرج البضاعة من المخزون للمورد، وتعكس حساب المورد/الذمم الدائنة).
5. **`CreditNote`**: نموذج إشعار دائن مستقل مرتبط بالفاتورة الأصلية المؤكدة لتنفيذ المرتجعات القانونية غير القابلة للتعديل حسب اشتراطات المرحلة الثانية للفوترة الإلكترونية (ZATCA Phase 2).

---

## 2. أثر حركة المرتجعات على المخزون والعهد والذمم المالية

### أ. مرتجع مبيعات العميل (`return_sale`)
عند تأكيد فاتورة مرتجع مبيعات عبر الخدمة [invoice_service.py](file:///c:/code/optics_tenant/osmBack/apps/sales/services/invoice_service.py):
* **أثر المخزون (`Stock`)**:
  * يتم تطبيق معامل إيجابي (`factor = +1`) لزيادة كمية البضاعة في الفرع تلقائياً: `stock.quantity_in_stock += quantity`.
  * يتم تسجيل حركة مخزنية مرجعية من نوع `'return'`.
* **أثر العهد والنقدية والذمم المالية (`Accounts / Custody`)**:
  * يُولد النظام قيد يومية عكسي تلقائياً عبر [entry_service.py](file:///c:/code/optics_tenant/osmBack/apps/accounting/services/entry_service.py):
    * **مدين (Dr)**: حساب الإيرادات / مردودات المبيعات `4100` (تخفيض الإيراد).
    * **مدين (Dr)**: حساب ضريبة القيمة المضافة مخرجات `2200` (تخفيض الالتزام الضريبي).
    * **دائن (Cr)**: حساب الذمم المدينة للعميل `1200` (في حالة الآجل) أو الصندوق/العهد النقدية `1100` (في حالة المرتجع النقدي).
* **أثر تكلفة البضاعة المباعة (`COGS`)**:
  * يُلغى جزء تكلفة البضاعة المباعة المقابل للبضاعة المرتجعة، بحيث يتم تسجيل:
    * **مدين (Dr)**: حساب المخزون السلعي `1300` (إعادة إثبات البضاعة كأصل).
    * **دائن (Cr)**: حساب تكلفة البضاعة المباعة `5100` (تخفيض مصروف التكلفة).

### ب. مرتجع مشتريات المورد (`return_purchase`)
عند تأكيد فاتورة مرتجع مشتريات للمورد:
* **أثر المخزون (`Stock`)**:
  * يفحص النظام أولاً توفر البضاعة في المستودع (`available_quantity >= item.quantity`).
  * يتم تطبيق معامل سلبي (`factor = -1`) لسحب البضاعة المرتجعة للمورد: `stock.quantity_in_stock -= quantity`.
  * يتم تسجيل حركة مخزنية مرجعية من نوع `'return_to_supplier'`.
* **أثر العهد والنقدية والذمم المالية (`Accounts / Custody`)**:
  * يُولد النظام قيد يومية عكسي:
    * **مدين (Dr)**: حساب الذمم الدائنة للموردين `2100` (تخفيض الديون المستحقة للمورد) أو البنك/الصندوق في حال الاسترداد النقدي.
    * **دائن (Cr)**: حساب المخزون السلعي `1300` (تخفيض أصول المخزون).
    * **دائن (Cr)**: حساب ضريبة المدخلات.

---

## 3. مدى تطبيق واستجابة الفرونت والباك اند (`Frontend & Backend Integration`)

### أ. الباك اند (`osmBack`)
* المعاملات تتم داخل معاملات ذرية مشددة (`@transaction.atomic`) لضمان عدم حدوث أي تضارب بين المستودع والمالية.
* إنشاء الإشعارات الدائنة ينشئ `UUID` و `ZATCA Tax Number` و `Hash` مخصص وتسجيلها في طابور Celery للتكامل مع هيئة الزكاة.

### ب. الفرونت اند (`osmFront/src/features/invoices`)
* في صفحة تفاصيل الفاتورة المعتمدة [InvoiceDetailPage.tsx](file:///c:/code/optics_tenant/osmFront/src/features/invoices/pages/InvoiceDetailPage.tsx):
  * الفواتير المؤكدة (Immutable) تظهر زراً مخصصاً لإصدار إشعار دائن `Issue Credit Note`.
  * يُفتح مربع الحوار المخصص [IssueCreditNoteDialog.tsx](file:///c:/code/optics_tenant/osmFront/src/features/invoices/components/IssueCreditNoteDialog.tsx) لتدوين سبب الارتجاع وطلب إصدار الإشعار.
* يتم تحديث تفاصيل الفاتورة والمخزون وقيود المحاسبة تلقائياً عند تأكيد الإشعار الدائن.

---

## ملخص جدول الحركة والآثار المترتبة

| نوع المرتجع | أثر المخزون | أثر نقدية العهدة/البنك | أثر ذمم العملاء/الموردين | أثر القيود المحاسبية |
| :--- | :--- | :--- | :--- | :--- |
| **مرتجع مبيعات (`return_sale`)** | **إعادة للمستودع** (+Qty) | **صرف نقدية** (إذا كان نقداً) | **تخفيض ذمة العميل** (إذا كان أجلاً) | **خصم الإيراد والضريبة، وإعادة البضاعة للأصول وخصم COGS** |
| **مرتجع مشتريات (`return_purchase`)** | **خصم وسحب** (-Qty) | **استرداد نقدية** (إذا كان نقداً) | **تخفيض دين المورد** (إذا كان أجلاً) | **خصم حساب الموردين وخصم قيمة المخزون والضريبة** |
| **إشعار دائن (`CreditNote`)** | **تحديث مخزني مرجعي** | **تسوية نقدية/آجلة** | **تعديل رصيد الفاتورة** | **قيد عكسي ZATCA معتمد** |
