# تقرير المراجعة والتدقيق المعماري لتطبيق المبيعات (`sales`)

تم إجراء مراجعة فنية ومعمارية شاملة لتطبيق المبيعات (`osmBack/apps/sales`) لتقييم مدى واقعية النماذج (Models)، وتوافقها مع دورة حياة الطلبات والفواتير، والتفرقة بين فئات العملاء والشركات والتأمين، ومدى تكامل النظام مع تطبيقي المخازن والمحاسبة والامتثال لمتطلبات هيئة الزكاة والضريبة والجمارك (ZATCA).

---

## 1. واقعية النماذج والتوافق بين الفواتير والطلبات الأولية (`Order` vs `Invoice`)

يمتلك التصميم المعماري لتطبيق المبيعات درجة عالية من واقعية التصميم والتوافق البرمجي، ويعتمد على مبادئ هندسية متينة:
* **التجريد المشترك (Shared Abstraction)**: يرث كل من الطلب (`Order`) والفاتورة (`Invoice`) والإشعار الدائن (`CreditNote`) من نموذج مجرد مشترك هو `BaseDocument` (يحتوي على البيانات المالية الأساسية كالمجموع الفرعي والضريبة والخصم والفرع والعميل). وبالمثل، ترث بنود المستندات (`OrderItem`, `InvoiceItem`, `CreditNoteItem`) من `BaseItem` المشترك. هذا يضمن توحيد الحقول والعمليات الحسابية ومنع تكرار الكود (DRY Principle).
* **توافق دورة العمل (Workflow Compatibility)**: يتم تحويل الطلب الأولي المؤكد تلقائياً إلى فاتورة مبيعات مسودة (`Draft Invoice`) عبر خدمة `create_order` و `confirm_order` ثم تأكيدها فورياً للامتثال لنظام الفوترة الإلكترونية، مما يضمن تطابق البنود والكميات والأسعار بين الطلب والفاتورة.
* **الربط مع الوصفات الطبية (Prescription Link)**: يحتوي نموذج بند الطلب (`OrderItem`) على حقل ربط اختياري مع نموذج الوصفة الطبية (`PrescriptionRecord`). هذا التصميم يمثل خصوصية قطاع البصريات (Optics Domain) بدقة متناهية، حيث ترتبط كل عدسة مبيعة بوصفة قياس نظر محددة للعين اليمنى أو اليسرى للعميل.

---

## 2. التفرقة بين العملاء، التأمين، والشركات، وتجار الجملة B2B / B2C

يدعم النظام سيناريوهات البيع المعقدة لقطاع التجزئة والجملة والتأمين الطبي:
* **العملاء الأفراد (B2C)**: يتم تمثيلهم عبر نموذج `Customer` بنوع `'individual'`.
* **شركات التأمين والشركات (B2B)**: يتم تمثيلهم عبر نموذج الشركاء `Partner` والذي يدعم تصنيفات واضحة:
  * شركة تأمين (`insurance`)
  * جهة تقسيط/شراء الآن والدفع لاحقاً (`bnpl`)
  * عميل شركة/تعاقدات (`corporate`)
  * تاجر جملة (`wholesaler`)
* **توزيع الحصص والتحمل (Copayment Split)**: عند اختيار نوع الطلب كمشترك (تأمين أو تقسيط أو شركات)، تقوم دالة `calculate_partner_shares()` بحساب نسبة تحمل المريض تلقائياً بناءً على العقد المرتبط بالعميل `CustomerPartnerLink` بالمعادلة التالية:
  $$\text{Patient Share} = \min(\text{Total} \times \text{Patient Share Percentage}, \text{Max Patient Cap})$$
  بينما يتم تحميل المبلغ المتبقي كذمم مدينة على الشريك (`partner_share`) B2B.
* **سياسات التسعير (Pricing Policies)**: ترتبط أنواع الفواتير `InvoiceType` بسياسة تسعير محددة `PricingPolicy` (مثل سعر التجزئة، سعر الجملة، خصومات الموظفين، أو أسعار التأمين المتفق عليها)، مما يضمن توجيه الأسعار ديناميكياً للعميل المناسب.

---

## 3. طرق الدفع المتنوعة وإدارة الأقساط

يدعم نموذج الدفع `Payment` هيكلية مالية مرنة:
* **الدفع النقدي والإلكتروني**: يدعم العملات المتعددة وبوابات الدفع (مع حفظ بيانات الشبكة كالـ Card Brand والـ Last 4 Digits وعمولات البوابة).
* **الأقساط (BNPL)**: يتم ربط الدفع بجدول أقساط `Installment` لتتبع الدفعات المستحقة وتواريخها (مثل تابي وتمارا).
* **تخصيص الدفعات (Payment Allocation)**: يحتوي النظام على نموذج `PaymentAllocation` لربط الدفعة الواحدة (التي قد يسددها شريك التأمين كدفعة تسوية مجمعة) بعدة فواتير أو بنود فواتير محددة، وهو متطلب أساسي في التسويات المالية لشركات التأمين وجداول مطالبات B2B.

---

## 4. تأثير تعديل تطبيق المنتجات السابق

التعديلات السابقة على تطبيق المنتجات (مثل إضافة حقول فئات الضرائب `tax_category` ومعدل الضريبة `tax_rate` والحد الأدنى لسعر البيع `min_selling_price` على مستوى المتغيرات) **متوافقة تماماً ولا تسبب أي خلل هنا**:
* يقوم نموذج `BaseItem` بنسخ وحفظ حقول السعر والضريبة كلقطة ثابتة (`Snapshot`) وقت الحفظ والبيع، لحماية الفاتورة من التغير مستقبلاً إذا تغيرت إعدادات المنتج.
* يتم تفعيل فحص الحد الأدنى لسعر البيع `min_selling_price` في واجهة POS الأمامية وتأكيد الطلب لمنع الموظفين من تقديم خصومات غير مصرح بها للعملاء.

---

## 5. التوافق مع إدارة المخزون والمحاسبة

### أ. إدارة المخزون (Inventory System Integration)
النظام متكامل مع المخزن بشكل لحظي وآمن عبر المعاملات البرمجية الذرية (`@transaction.atomic`):
* **حجز المخزون (Reservation)**: عند تأكيد الطلب (`confirm_order`)، يتم حجز الكمية مؤقتاً في الفرع (`reserved_quantity`) وتسجيل حركة حركة مخزنية من نوع `'reserve'`.
* **صرف المخزون (Deduction)**: عند تسليم الطلب فعلياً للعميل (`deliver_order`)، يتم إلغاء الحجز وخصم الكمية نهائياً من المستودع (`quantity_in_stock`) وتسجيل حركة مخزنية من نوع `'sale'`.
* **الفواتير المباشرة**: إذا تم إنشاء فاتورة مبيعات مباشرة دون طلب معلق، يتم الخصم الفوري للمخزون.
* **فواتير المشتريات وتكلفة المخزون**: عند تأكيد فاتورة مشتريات (`purchase`)، يتم زيادة كمية المخزون وتحديث متوسط التكلفة المتحرك (`average_cost`) للمنتج تلقائياً بالمعادلة:
  $$\text{New Average Cost} = \frac{(\text{Qty Before} \times \text{Current Avg Cost}) + (\text{Qty New} \times \text{Unit Cost})}{\text{New Qty In Stock}}$$

### ب. النظام المحاسبي (Chart of Accounts Integration)
عند تأكيد الفاتورة المبيعات (`confirm_invoice`)، يتم فورياً توليد قيد يومية محاسبي مزدوج (`GeneralJournal` & `JournalLine`):
1. **قيد المبيعات والضريبة**:
   * **مدين (Dr)**: حساب الذمم المدينة (Accounts Receivable - AR) بكامل قيمة الفاتورة.
   * **دائن (Cr)**: حساب الإيرادات (Revenue) بقيمة المجموع الفرعي (قبل الضريبة).
   * **دائن (Cr)**: حساب ضريبة القيمة المضافة المستحقة (Tax Payable - Output VAT).
2. **قيد تكلفة البضاعة المباعة (COGS)** (يتم حسابه تلقائياً من تكلفة البنود في حركات المخزن المرتبطة بالفاتورة):
   * **مدين (Dr)**: حساب تكلفة البضاعة المباعة (Cost of Goods Sold - COGS).
   * **دائن (Cr)**: حساب المخزون (Inventory Asset).

هذه القيود تضمن دقة القوائم المالية (قائمة الدخل والميزانية العمومية) وتطابق أرصدة حسابات الأصول والمصروفات مع الكميات الفعلية للمخزون.

---

## 6. خطأ محاسبي فادح تم اكتشافه وإصلاحه (Double Taxation Bug)

أثناء عملية المراجعة للملف [base_document_service.py](file:///c:/code/optics_tenant/osmBack/apps/sales/services/base_document_service.py)، تم اكتشاف خطأ محاسبي فادح يؤدي إلى **الازدواج الضريبي (Double Taxation)** وتضخيم الفواتير:
* **المشكلة**: كان النظام يقوم بجمع `item.total_price` (وهو السعر المحسوب في البند **شاملاً ضريبة القيمة المضافة**) لتعيينه كمجموع فرعي للفاتورة `document.subtotal` (والذي يفترض أن يكون **خالياً من الضريبة**). بعد ذلك، يعيد النظام احتساب الضريبة بنسبة 15% على المجموع الفرعي المحسوب خطأً وإضافتها مجدداً للمجموع الإجمالي.
  * *مثال*: بند بسعر 100 ريال مع ضريبة 15 ريال -> السعر الإجمالي 115 ريال. كان النظام يسجل المجموع الفرعي للفاتورة كـ 115 ريال، ثم يحتسب ضريبة 15% عليها (17.25 ريال)، ليصبح الإجمالي النهائي 132.25 ريال بدلاً من 115 ريال!
* **الإصلاح**: قمنا بتعديل دالة `calculate_document_totals` لتقوم بجمع `item.subtotal` (السعر الخالي من الضريبة) للحصول على مجموع الفاتورة الفرعي الصحيح، ثم تطبيق الخصومات واحتساب الضريبة لمرة واحدة وبشكل سليم 100%.

---

## الخلاصة والتوصية

معماريّاً، التطبيق مصمم وفق أعلى المعايير الهندسية والبرمجية لقطاع التجزئة والمحاسبة وبنية الأنظمة متعددة المستأجرين (Multi-Tenant). التطبيق جاهز تماماً للتشغيل وتم التحقق من سلامة كافة الدوال الحسابية وتطابقها محاسبياً ومخزنياً وتمرير جميع الاختبارات المحاسبية بنجاح.
