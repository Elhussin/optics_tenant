# تقرير مراجعة وتدقيق تطبيق الأدوار والصلاحيات (`roles`)

تمت مراجعة تطبيق الأدوار والصلاحيات في الفرونت اند (`osmFront/src/features/roles`) لتقييم آليات ربط الصلاحيات، وإنشاء وإلغاء الأدوار، وحذف الصلاحيات أو إضافتها، وعرض الصلاحيات المرتبطة بدور معين، بالإضافة إلى فحص قيود منع تكرار الأدوار والصلاحيات على مستويي قاعدة البيانات والواجهات.

---

## 1. بنية وتصميم تطبيق الأدوار والصلاحيات (`Roles Manager`)

يتمحور التطبيق حول واجهة إدارة مركزية [RolesManager.tsx](file:///c:/code/optics_tenant/osmFront/src/features/roles/components/RolesManager.tsx) ويعتمد على المعايير المعمارية التالية:
* **عزل الخطافات (Hooks Separation)**: يتم جلب البيانات وإجراء العمليات بالكامل عبر خطاف مخصص [useRoles.ts](file:///c:/code/optics_tenant/osmFront/src/features/roles/hooks/useRoles.ts) والذي يتكامل مع `React Query` لتنفيذ عمليات الجلب (`users_roles_list`, `users_permissions_list`) والتعديل والحذف وتحديث الكاش تلقائياً.
* **واجهة الخطوات والتنقل**:
  * على اليسار: يتم عرض قائمة الأدوار (`System Roles`) مع عدد الصلاحيات الممنوحة لكل منها وزر التعديل والحذف.
  * على اليمين: عند النقر على دور معين، يتم تحميل تفاصيله وصلاحياته بشكل تفاعلي وإظهار لوحة الصلاحيات المتاحة.

---

## 2. آليات ربط، وإضافة، وإلغاء الصلاحيات

* **تعديل الصلاحيات الفردية (Permission Toggling)**:
  * يتم عرض الصلاحيات في مجموعات منطقية بناءً على تصنيفاتها (مثل إدارة المنتجات، الفواتير، الوصفات الطبية) باستخدام دالة المخطط المحسن `getPermissionDetails(p.code)`.
  * عند النقر على كرت الصلاحية أو صندوق الاختيار الخاص بها، يتم تشغيل دالة `handleTogglePermission` والتي تبحث في المصفوفة الحالية:
    * إذا كانت الصلاحية موجودة مسبقاً: يتم إزالتها (إلغاء الصلاحية).
    * إذا لم تكن موجودة: يتم إضافتها (إعطاء الصلاحية).
  * تُرسل القائمة الجديدة مباشرة للباك اند كقائمة معرفات عدديّة عبر حقل `permission_ids` للربط الفوري.

* **إنشاء وإلغاء الأدوار (Roles CRUD)**:
  * **الإنشاء والتعديل**: يتم عبر صندوق الحوار المخصص [RoleDialog.tsx](file:///c:/code/optics_tenant/osmFront/src/features/roles/components/RoleDialog.tsx) الذي يوفر نموذجاً تفاعلياً لإدخال اسم الدور ووصفه وحالته التنشيطية (`is_active`).
  * **الإلغاء (الحذف)**: يتم عبر دالة الحذف بالتحقق أولاً عبر رسالة تأكيد للمستخدم، ثم إرسال طلب تدمير للباك اند تحديث واجهة الاستعلامات.

---

## 3. حماية منع التكرار (Preventing Duplicates)

تم تأمين النظام ضد تكرار الأدوار أو تكرار الصلاحيات على مستويين متكاملين:

### أ. على مستوى قاعدة البيانات والباك اند (`osmBack/apps/users/models.py`)
1. **عدم تكرار اسم الدور**: حقل الاسم في موديل `Role` تم تعريفه بـ `unique=True` لمنع إنشاء دورين بنفس الاسم تماماً.
   ```python
   name = models.CharField(max_length=50, unique=True, verbose_name=_('Name'))
   ```
2. **عدم تكرار كود الصلاحية**: حقل الكود لموديل `Permission` تم تعريفه بـ `unique=True` لمنع تكرار تعريف الصلاحيات.
   ```python
   code = models.CharField(max_length=100, unique=True, verbose_name=_('Code'))
   ```
3. **منع تكرار ربط الصلاحية بالدور**: نموذج علاقة الربط المتعدد `RolePermission` يفرض قيد فريد مشترك يمنع ربط نفس الصلاحية بنفس الدور أكثر من مرة.
   ```python
   class Meta:
       unique_together = ('role', 'permission')
   ```

### ب. على مستوى الفرونت اند والواجهات
1. **تجنب التكرار في المصفوفات**: تعتمد دالة `handleTogglePermission` على التبديل الفريد (Toggle):
   ```typescript
   const newPermIds = currentPermIds.includes(permissionId)
     ? currentPermIds.filter((id) => id !== permissionId)
     : [...currentPermIds, permissionId];
   ```
   هذا يضمن أن المعرف العددي للصلاحية لا يمكن أن يتكرر داخل مصفوفة التحديث أبداً.
2. **مفاتيح العرض (Keys)**: يتم تمرير المفاتيح الفرعية لكل بند مبيعات أو دور بناءً على الـ `id` الفرعي للمستند من قاعدة البيانات لمنع أخطاء التكرار في واجهة React.

---

## نتيجة البناء والتجميع (TypeScript Compilation)
تم التأكد من صحة الكود وسلامته وخلوه من أخطاء التجميع:
* فحص البناء للفرونت اند كاملًا اكتمل بنجاح دون وجود أي أخطاء برمجية في مجلد الأدوار والصلاحيات.
